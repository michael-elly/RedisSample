Download and install Redis locally in c:\redis

To start Redis server locally run this command:
c:\Redis\redis-server.exe

To start the Redis CLI open another command and run this command:
c:\redis\redis-cli.exe

